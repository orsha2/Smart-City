repos:
  - repo: local
    hooks:
      - id: check-commit-message-from-branch
        name: Check commit message prefix from branch name
        entry: scripts/check_commit_msg.sh
        language: system
        stages: [commit-msg]